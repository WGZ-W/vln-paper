


## 问题
1. 历史帧使用方式，无人机和室内机器人的有何区别？
2. 为什么要保存图像，而不是直接区分指令的执行部分和保存动作？
3. 

# 关键问题

- 图文检索？
- 如何获取指令中的地标，与图像相对应，保留匹配的历史帧，？保留关键的历史帧的动作，？
- 使用所有的历史帧信息冗余，
- 抽样选取历史帧会遗失关键信息，
- 动作发生改变的帧确实可以当作关键帧，但是直行的关键帧不容易获取，且不能判断执行到指令的什么位置？



# 论文阅读

## ViLaMP

## NavAgent
导航文本地标提取，
图像框提取视觉地标，
特征匹配，选出分数最高的，
需要维护一个拓扑图，
这种方法是最基础的思考方式，合理的将人对于导航的方式实现出来，

## OpenFly
选择动作改变的帧作为关键帧

## NaVid
借鉴于 LLaMA-VID ，使用两类 Tokens ，
使用所有历史帧，

## VILA
固定间隔采样 

## NaVILA
先确定当前帧，然后从历史图像中采样，且必须包含第一帧 

## Uni-NaVid
- 分组，当前帧，短期记忆，长期记忆，进行不同程度的池化
- 新的帧进入，老帧进行池化，长期记忆进行合并